import jQuery from "jquery";
import "bootstrap";

import "jquery.easing/jquery.easing.js"

var Feature = Feature || {};
Feature.HeroSlider = Feature.HeroSlider || {};

Feature.HeroSlider.carousel = {
   
    init: () => {
        
    }
}

Feature.HeroSlider.carousel.init();
